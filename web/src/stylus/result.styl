@import "_layout";
@import "_tabs";

.page {
    .separation {
        height: .75rem;
        background: #F5F5F5;
    }
    .result {
        padding: 1rem;
        .type {
            border-left: .25rem solid #333;
            text-indent: 1rem;
        }
    }
}