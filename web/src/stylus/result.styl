@import "_layout";
@import "_tabs";
@import "_list";

.page {
    .main {
        padding: .75rem 1rem;
        .type {
            border-left: .25rem solid #333;
            text-indent: 1rem;
        }
    }
}